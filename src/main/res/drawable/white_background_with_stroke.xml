<!-- res/drawable/white_background_with_stroke.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- White background -->
    <stroke
        android:width="2dp"
        android:color="@android:color/black" /> <!-- Black stroke -->
    <corners android:radius="8dp" /> <!-- Optional: Rounded corners -->
</shape>